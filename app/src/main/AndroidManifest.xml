<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uzt.edzsterv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.edzesterv.EdzestervMainActivity" />
        <activity android:name=".activities.edzesterv.EdzestervSablonActivity" />
        <activity android:name=".activities.edzesterv.EdzestervEgyeniActivity" />
        <activity android:name=".activities.etrend.EtrendMainActivity" />
        <activity android:name=".activities.etrend.EtrendEgyeniActivity" />
        <activity android:name=".activities.etrend.EtrendSablonActivity"></activity>
    </application>

</manifest>